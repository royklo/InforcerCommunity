# Inforcer Module Findings

Track what was wrong, what was done to fix it, and how to test. Update the "What was done" column when implementing fixes so the guardian skill stays self-learning.

## Summary of all changes (this thread)

Use this list to verify everything from the recent fix pass (Get-InforcerTenant -TenantId, Get-InforcerAlignmentDetails -TenantId, API errors, etc.):

| # | Area | What was wrong | What was fixed | Quick check |
|---|------|----------------|----------------|-------------|
| 1 | **Get-InforcerTenant -TenantId** | Returned full tenant list and duplicate rows instead of one tenant. | Always use GET /beta/tenants only. Collect with @($response). Dedupe by ClientTenantId with Where-Object; filter with Where-Object when -TenantId set. Same for JsonObject. | `Get-InforcerTenant -TenantId 12274` → one row. `Get-InforcerTenant` → no duplicate clientTenantIds. |
| 2 | **Get-InforcerAlignmentDetails -TenantId** | -TenantId did not filter correctly; parse/brace errors. | Filter logic fixed; early return when tenant not in list; nested logic moved to helper Add-ChildTenantIdsFromAlignments; braces/indentation fixed. When -TenantId is baseline owner, include tenants aligned TO that baseline. | `Get-InforcerAlignmentDetails -TenantId 103` (scored tenant) and `-TenantId 107` (baseline owner) both return expected rows. No parse errors. |
| 3 | **API errors (forbidden/not found)** | User saw "API returned an empty or invalid response" when API returned success:false. | Invoke-InforcerApiRequest uses Invoke-RestMethod, checks success:false and shows: "You don't have permission…" (forbidden), "Tenant or resource not found." (not found). HTTP status code in ErrorId for programmatic handling. | Get-InforcerTenantPolicies -TenantId (no-access) → friendly message. Get-InforcerTenant -TenantId 99999 (nonexistent) → "Tenant or resource not found." |
| 4 | **Get-InforcerAuditEventType** | Showed in Get-Command. | ~~Removed from manifest.~~ Later renamed to `Get-InforcerSupportedEventType`, moved to Public/, and re-exported. | `Get-InforcerSupportedEventType` works. Old name no longer exists. |
| 5 | **Trigger workflow** | "No event triggers defined in on" in module repo. | trigger-publish.yml renamed to trigger-publish.yml.disabled so GitHub ignores it. | No workflow error on push. |
| 6 | **Docs / guardian** | Phase 3 said Get-InforcerTenant uses GET /beta/tenants/{id}. | FINDINGS and inforcer-unified-guardian updated: Get-InforcerTenant always uses list + Where-Object filter; do not use GET /beta/tenants/{id}. | See FINDINGS row and guardian "Response shapes". |
| 7 | **Get-InforcerUser** | New cmdlet needed for /beta/tenants/{id}/users endpoints. | Added Get-InforcerUser with List/ById ParameterSets, auto-pagination, UserSummary+User PSTypeNames, ListControl views. | `Get-InforcerUser -TenantId 646` and `-UserId "..."` both return data. |
| 8 | **Performance** | Invoke-WebRequest + ConvertFrom-Json was slow. | Switched to Invoke-RestMethod. Added non-JSON response validation. | All cmdlets faster. |
| 9 | **Tenant name resolution** | -TenantId only accepted numeric ID or GUID. | Resolve-InforcerTenantId now also matches tenantFriendlyName/tenantDnsName. Get-InforcerTenant passes -TenantData to avoid double API call. | `Get-InforcerUser -TenantId "Contoso"` works. |
| 10 | **PR review fixes** | Pipeline binding, streaming, error consistency, JsonObject aliases, duplicate API call, help docs. | 6 fixes from Copilot agent review — see detailed findings below. | 23/23 Pester tests pass. |
| 11 | **Settings Catalog runtime fetch** | Bundled 62.5 MB settings.json was static at ship time and too large for module package. | New `IntuneSettingsCatalogData` repo with nightly Graph fetch + GitHub Releases. Module auto-downloads and caches with 24h TTL via `Get-InforcerSettingsCatalogPath`. | `rm -rf ~/.inforcercommunity/data/; InModuleScope InforcerCommunity { Get-InforcerSettingsCatalogPath -Verbose }` → downloads + caches. |

---

| Finding                                                                               | What was done to solve it                                                                                                                                                                                                                                                   | How to test                                                                                                                                                                         |
| ------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Get rid of the dotnet folder and everything that has to do with it                    | Removed dotnet build job from GitHub workflow; removed dotnet paths from .gitignore; updated all agent docs, README, CONTRIBUTING, Powershell/README, Inforcer.psm1/psd1 and Private script comments to remove dotnet references. Script module is the only implementation. | Run CI; confirm only script-module Pester tests run. No dotnet build.                                                                                                               |
| Update all the md files                                                               | README, CONTRIBUTING, Powershell/README.md, and agent docs updated to reflect script-only module. FINDINGS.md and guardian skill added.                                                                                                                                     | Read-through README, CONTRIBUTING, Powershell/README.md; verify links and that no dotnet instructions remain.                                                                       |
| Make sure the output of -OutputType is json with Depth 100 when "JsonObject" is asked | Invoke-InforcerApiRequest and Filter-InforcerResponse already use ConvertTo-Json -Depth 100. Get-InforcerAuditEvent uses Depth 100 for JsonObject. No code path uses a lower depth.                                                                                         | Run each Get-* cmdlet with `-OutputType JsonObject`; assert output is valid JSON and nested objects are fully serialized (no truncation).                                           |
| Check the disconnect output                                                           | Disconnect-Inforcer now always outputs a string: "Disconnected from Inforcer API." when session was active, "No active session to disconnect." when no session.                                                                                                             | (1) Connect then Disconnect-Inforcer; expect string "Disconnected from Inforcer API.". (2) Disconnect-Inforcer without connecting; expect string "No active session to disconnect." |
| Get-InforcerAlignmentDetails shows cached value after updating in portal                | Table format now uses /beta/alignmentScores as the data source (same as Raw), then joins tenant names from /beta/tenants. No in-memory caching.                                                                                                                             | Update alignment in portal, run `Get-InforcerAlignmentDetails` (Table); expect updated score. Compare with `Get-InforcerAlignmentDetails -Format Raw`.                                  |
| lastComparisonDateTime is missing in Get-InforcerAlignmentDetails                       | LastComparisonDateTime added to the Table row (from alignment summary lastComparisonDateTime). Raw format already had it via Add-InforcerPropertyAliases.                                                                                                                   | `Get-InforcerAlignmentDetails -Format Table` and `-Format Raw`; both should show LastComparisonDateTime (or lastComparisonDateTime in JSON) where API provides it.                    |
| PolicyDiff is an empty property in Get-InforcerTenant                                 | Added PascalCase alias PolicyDiff from policyDiff. Added PolicyDiffFormatted built from recentChanges when present (structured sections: Changed/Added/Removed). When API returns policyDiff or recentChanges, output is populated.                                         | Get-InforcerTenant for a tenant known to have policy changes; confirm PolicyDiff or PolicyDiffFormatted is populated when API provides data.                                        |
| Validate Connect-Inforcer: invalid key should not report Connected                   | Connect-Inforcer validates the key with GET /beta/baselines before setting session. On failure, session is not set. Connect output is Status, Region, BaseUrl, ConnectedAt only.                                                                                             | Connect with an invalid key or wrong endpoint; expect error and no Connected output. Connect with valid key; output shows Status, Region, BaseUrl, ConnectedAt.                   |
| PolicyDiff format: make it structured (Changed/Added/Removed) not plain text          | PolicyDiffFormatted property added when recentChanges exists: formatted with sections changedPolicies/addedPolicies/removedPolicies (or changed/added/removed) and bullet lines.                                                                                            | Get-InforcerTenant for a tenant with changes; PolicyDiffFormatted should show structured sections and lines like the policy change report.                                          |
| Licenses is now an array; show comma-separated in output                              | Tenant objects: licenses array is replaced by a single comma-separated string (values from each item's .sku, .name, or ToString()). Property `licenses` holds the string. LicensesDisplay was removed; use `licenses` only.                                                                 | Get-InforcerTenant; select .licenses — expect a string like "PREMIUM". LicensesDisplay is not present.                                                                               |
| Get-InforcerTenantPolicies: always use PolicyName (consistent properties)             | PolicyName is always set from displayName, name, friendlyName, or "Policy {id}". name, displayName, and friendlyName are removed from output so only PolicyName (and FriendlyName alias) show. Add-InforcerPropertyAliases and EnrichPolicyObject both apply.               | Get-InforcerTenantPolicies -TenantId &lt;id&gt;; each object has PolicyName; no name, displayName, or friendlyName properties.                                                         |
| Remove metadata property from Get-InforcerAuditEvent output                           | After flattening metadata onto AuditEvent (ClientIpv4, ClientIpv6, UserName, UserDisplayName), the metadata property is removed from the object so it does not appear in cmdlet output.                                                                                     | Get-InforcerAuditEvent; output objects should not have a `metadata` property; flattened fields (e.g. ClientIpv4, UserName) should still be present.                                 |
| Always update get-help so all cmdlets have full understanding including examples      | Comment-based help reviewed and updated for all Public cmdlets: .SYNOPSIS, .DESCRIPTION, .PARAMETER, .EXAMPLE, .OUTPUTS, .LINK.                                                                                                                                             | Run Get-Help -Full for each exported function; confirm all sections and at least one example.                                                                                       |
| Update README, CONTRIBUTING and other md files                                        | README, CONTRIBUTING, Powershell/README.md and agent docs updated for script-only module. FINDINGS.md and guardian skill added.                                                                                                                                             | Read-through and link check.                                                                                                                                                        |
| Connect-Inforcer must not return Connected when API key is invalid for endpoint       | Before returning Connected, Connect-Inforcer calls GET /beta/baselines to validate the key. On 401 or other failure, session is not set and an error is returned.                                                                                                             | Connect with an invalid key or a key that does not match the endpoint; expect error and no Connected output. Next cmdlet should not run (no session).                           |
| Get-InforcerAlignmentDetails -OutputType JsonObject does nothing with default Table     | When Format is Table (default), -OutputType JsonObject now converts the alignment table to JSON (Depth 100) and returns that string.                                                                                                                                         | Get-InforcerAlignmentDetails -TenantId 107 -OutputType JsonObject; expect a JSON string, not silent/no output.                                                                      |
| Invoke-InforcerApiRequest null response crash                                         | Guard added: if API returns a response that parses to $null or missing, skip indexing rawResponse and return with a clear error instead of "Cannot index into a null array".                                                                                                | (Rare) If API returns empty/invalid body, expect "API returned an empty or invalid response" instead of a crash.                                                                     |
| Get-InforcerTenantPolicies / Get-InforcerAlignmentDetails returning no output           | Invoke-InforcerApiRequest now unwraps response.data whenever the property exists (not only when success is present). Ensures API responses like { "data": [ ... ] } are returned as the array so cmdlets emit objects.                                                             | Get-InforcerTenantPolicies -TenantId 107; Get-InforcerAlignmentDetails; Get-InforcerAlignmentDetails -TenantId 107 -OutputType JsonObject — all should produce output when API has data.   |
| Get-InforcerAlignmentDetails no output: API returns flat alignmentScores array         | /beta/alignmentScores returns flat `data` array. Table uses flat format (one row per item). Tenant filter applied only when -TenantId/-Tag is passed. **Baseline owner fix:** when -TenantId is a baseline owner (e.g. 107), code also includes tenants whose `alignmentSummaries.alignedBaselineTenantId` matches, so you see scores for tenants aligned TO that baseline.                                           | `Get-InforcerAlignmentDetails` (all), `-TenantId 103` (scored tenant), `-TenantId 107` (baseline owner) — all should return rows. |
| Module efficiency (Phase 1): region lookup, dead code, Write-Verbose, StreamReader, help, licenses | Region lookup in Get-InforcerBaseUrl uses hashtable indexing. Dead BaseObject block removed from Add-InforcerPropertyAliases. API key/URI logging in Test-InforcerConnection moved to Write-Verbose. StreamReader in Invoke-InforcerApiRequest wrapped in try/finally. Get-InforcerTenantPolicies help documents JSON Depth 100. Licenses iteration unified to single branch in Add-InforcerPropertyAliases. | Run Get-InforcerBaseUrl -Region uk; Get-InforcerTenant; Test-InforcerConnection (-Verbose shows key info); Get-InforcerTenantPolicies -TenantId &lt;id&gt; -OutputType JsonObject; Get-Help Get-InforcerTenantPolicies lists Depth 100. |
| Module efficiency (Phase 2): deduplication | Get-InforcerAuditEvent uses Get-InforcerSupportedEventType (no duplicate event-type parsing). Get-InforcerTenant builds tenant predicate once. PolicyName only in Add-InforcerPropertyAliases. ConvertFrom-InforcerSecureString private helper used by Connect, Test-InforcerConnection, Invoke-InforcerApiRequest. ConvertTo-InforcerArray used in Get-InforcerAlignmentDetails; FormatAlignmentScore shared for flat/nested. | Get-InforcerAuditEvent; Get-InforcerTenant -TenantId &lt;id&gt;; Get-InforcerTenantPolicies; Connect/Test-InforcerConnection; Get-InforcerAlignmentDetails — all behave as before with less duplicate code. |
| Module efficiency (Phase 3): API efficiency | Get-InforcerAlignmentDetails fetches /beta/tenants only when needed (nested format or -TenantId or -Tag). Resolve-InforcerTenantId accepts -TenantData to avoid second /beta/tenants call when caller already has tenant list. Get-InforcerTenant always uses GET /beta/tenants then client-side filter (see finding "Get-InforcerTenant -TenantId returns full list"); do not use GET /beta/tenants/{id}. | Get-InforcerTenant -TenantId 142 (list + filter). Get-InforcerAlignmentDetails with no params (no /beta/tenants when flat). Get-InforcerAlignmentDetails -TenantId &lt;GUID&gt; -Format Table (tenants fetched once, passed to Resolve-InforcerTenantId). |
| Module efficiency (Phase 4): edge cases | Filter-InforcerResponse: try/catch around ConvertFrom-Json with clear error; null guard when JSON parses to $null; array filter skips null elements. | Pass invalid JSON to a cmdlet that uses Filter-InforcerResponse with JsonObject; expect error. JSON "null" input returns 'null' without pipeline errors. |
| API returns success:false (forbidden/not found) but user sees "API returned an empty or invalid response" | Invoke-InforcerApiRequest now uses Invoke-WebRequest and parses the response body once. Before unwrapping .data, it checks for success: false and emits a single, clean message: "You don't have permission to access this tenant or resource." (forbidden), "Tenant or resource not found." (not found), or the API message otherwise. No raw JSON or stack in the error. Aligns with guardian: non-terminating WriteError, return. | Get-InforcerTenantPolicies -TenantId 107 (or any tenant you lack access to): expect "You don't have permission to access this tenant or resource." Get-InforcerTenant -TenantId 99999 if 99999 does not exist: expect "Tenant or resource not found." or API message. No "API returned an empty or invalid response." |
| Get-InforcerTenant -TenantId returns full list and duplicate rows instead of one tenant | Always use GET /beta/tenants (list endpoint) only; do not use GET /beta/tenants/{id}. Collect response with @($response) so we always have an array. Deduplicate by ClientTenantId (or clientTenantId) with Where-Object; when -TenantId is specified, filter with a single Where-Object so only that tenant is returned. JsonObject path also filters and returns only matching tenant(s). | Import module from repo (e.g. .\module\InforcerCommunity.psd1 -Force). Get-InforcerTenant -TenantId 12274 — expect one row (Inforcer Blueprint Library, clientTenantId 12274). Get-InforcerTenant (no -TenantId) — deduped list, no duplicate clientTenantIds. |
| Get-InforcerAlignmentDetails: add alignment details support (-BaselineId) | Added -BaselineId parameter (GUID or friendly name) to Get-InforcerAlignmentDetails. When both -TenantId and -BaselineId are provided, calls GET /beta/tenants/{id}/alignmentDetails?customBaselineId={guid}. New private helper Resolve-InforcerBaselineId resolves name→GUID via GET /beta/baselines. New AlignmentDetail ObjectType in Add-InforcerPropertyAliases. Table format outputs metrics summary + per-policy rows. Raw and JsonObject formats supported. Existing behavior without -BaselineId is unchanged. | `Get-InforcerAlignmentDetails -TenantId 139 -BaselineId "Provision M365"` → metrics row + policy rows. `-Format Raw` → full PSObject with aliases. `-OutputType JsonObject` → JSON Depth 100. Without -BaselineId → same as before. `-BaselineId` without `-TenantId` → error. |
| Module optimization (Phase 5): comprehensive performance pass | **Test-InforcerSession:** Replaced BSTR alloc/free with `SecureString.Length` check (runs every API call). **Resolve-InforcerBaselineId:** Merged two loops (case-sensitive + case-insensitive) into single pass; prefer exact, remember case-insensitive; GUID regex → `[guid]::TryParse()`. **Resolve-InforcerTenantId:** GUID regex → `[guid]::TryParse()`; digit regex → `[int]::TryParse()`. **Add-InforcerPropertyAliases:** Licenses `+=` → `List[string]`; PolicyDiffFormatted `+=` → `List[string]`; Property Remove+Add → `.Value` assignment for PolicyName, licenses; FriendlyName alias moved after hide-loop (PSObject.Properties is case-insensitive); cached `$alignVal` for AlignmentDetail; cleaned metadata IPv4/IPv6 key lookup. **Get-InforcerAuditEvent:** `$typeBatches +=` → `List[object]`; `\| Out-Null` → `$null =`; removed `ForEach-Object { $_ }` output no-op. **Get-InforcerTenant:** Regex validation → `TryParse`; `\| Out-Null` → `$null =`; dedup rewritten from `Where-Object` pipeline to `foreach` with `ContainsKey`; removed pipeline no-op output. **Invoke-InforcerApiRequest:** Removed redundant `ForEach-Object { $_ }` enumeration; pre-compiled regex for API key redaction. **Get-InforcerAlignmentDetails:** Tag filter `.IndexOf()` → `-like` with pre-built pattern; all `\| Out-Null` → `$null =`; removed all `ForEach-Object { $_ }` output no-ops. **Get-InforcerTenantPolicies:** EnrichPolicyObject `$names +=` → `List[string]`; tags Remove+Add → `.Value` assignment; `\| Out-Null` → `$null =`; removed pipeline no-op output. **Get-InforcerBaseline:** Removed pipeline no-op output. **Bug found during testing:** FriendlyName alias was lost because PSObject.Properties is case-insensitive — `Properties["FriendlyName"]` matched original `friendlyName`, causing the alias skip. Fixed by moving alias addition after the hide-loop that removes originals. **Pester tests expanded from 19 to 50**, covering all private helpers with mock data. | `Invoke-Pester ./Tests/Consistency.Tests.ps1` → 50/50 pass. All cmdlets load, bind parameters, and handle no-session correctly. Private helpers verified: Test-InforcerSession (5 edge cases), Resolve-InforcerTenantId (3 cases), Resolve-InforcerBaselineId (5 cases incl. exact-vs-insensitive preference), Add-InforcerPropertyAliases (8 cases covering all ObjectTypes), Filter-InforcerResponse (4 cases), ConvertTo-InforcerArray (3 cases). |
| PolicyDiffFormatted dead code removed | `recentChanges` is an integer (count), not a structured object — the PolicyDiffFormatted code in Add-InforcerPropertyAliases never fired. Removed PolicyDiffFormatted from: Add-InforcerPropertyAliases.ps1, Get-InforcerTenant.ps1 (Select-Object), README.md, CMDLET-REFERENCE.md, and Consistency.Tests.ps1. | `Get-InforcerTenant` output has PolicyDiff but no PolicyDiffFormatted. 50/50 Pester tests pass. |
| 4 new audit event types from live API | Added `reportQueued`, `scheduleCreate`, `scheduleUpdate`, `scheduleDelete` to both the tab completer ValidateSet in Get-InforcerAuditEvent.ps1 and the cached list in Get-InforcerSupportedEventType.ps1. Both lists alphabetically sorted. | `Get-InforcerAuditEvent -EventType <Tab>` shows all types incl. new ones. `Get-InforcerSupportedEventType` returns sorted list with new types. |
| API schema drift detection | Created `scripts/Test-ApiSchemaChanges.ps1` — connects to live API, captures property schemas from all 7 endpoints, diffs against `docs/api-schema-snapshot.json`. Reports new/removed/type-changed properties. `-Update` regenerates snapshot. Nightly GitHub Action in Powershell-Module-Automation repo runs against UAT, auto-creates PRs on InforcerCommunity with `api-drift` label. Workflow bootstraps snapshot on first run (creates baseline if `docs/api-schema-snapshot.json` missing). | `./scripts/Test-ApiSchemaChanges.ps1` → "No schema changes detected." (exit 0). Add fake property to snapshot → shows "REMOVED" (exit 2). |
| Format.ps1xml — ALL views use ListControl, no default tables | Added PSTypeNames and Format.ps1xml ListControl views for all object types. **Tenant**: 8 key properties (hides policyDiff, tags, alignmentSummaries). **Baseline**: Name, Id, Owner, OwnerTenantId, Members (shows tenant names with IDs), AlignedThreshold, SemiAlignedThreshold, Mode. **Policy**: PolicyName, Product, PrimaryGroup, SecondaryGroup, Platform, ReadOnly. **AlignmentScore**: Tenant, TenantId, AlignmentScore, BaselineName, LastComparisonDateTime. **AlignmentDetailPolicy**: 7 policy fields (PolicyName, AlignmentStatus, Product, PrimaryGroup, SecondaryGroup, InforcerPolicyTypeName, Tags). **AuditEvent**: EventType, Code, User, Timestamp, Message, ClientIpv4, UserName, UserDisplayName. User can pipe to `\| ft` for table view when desired. `Select-Object *` shows all properties. | `Get-InforcerTenant` → clean list. `Get-InforcerBaseline` → list with member names. `Get-InforcerTenantPolicies -TenantId 646` → list. `Get-InforcerAlignmentDetails` → list. All cmdlets default to list format. `\| ft` works for table. |
| AlignmentDetails Table with -BaselineId: metrics separated from policy rows | Previously, metrics (TenantName, AlignmentScore, TotalPolicies, Aligned, etc.) were repeated on EVERY policy row. Now emits `InforcerCommunity.AlignmentDetailMetrics` summary first, then `InforcerCommunity.AlignmentDetailPolicy` per-policy rows with only policy-relevant fields (PolicyName, AlignmentStatus, Product, PrimaryGroup, SecondaryGroup, InforcerPolicyTypeName, Tags). | `Get-InforcerAlignmentDetails -BaselineId "Tier 0"` → metrics summary first, then policy list without repeated metrics. |
| Get-InforcerBaseline duplicate output bug | `Add-InforcerPropertyAliases` returns the modified object to the pipeline. In Get-InforcerBaseline, the call was not suppressed with `$null =`, so each baseline was output twice: once from the unsuppressed function call, once from `$result`. Fixed by adding `$null =` before both `Add-InforcerPropertyAliases` calls. | `Get-InforcerBaseline` → 4 baselines (not 8). Clean table format. |
| Get-InforcerAuditEventType renamed to Get-InforcerSupportedEventType | Renamed for clarity: the cmdlet returns supported event types, not audit events themselves. File moved from Private/ to Public/ (was already exported). Updated: function name, file name, manifest FunctionsToExport, Get-InforcerAuditEvent internal call, Consistency.Tests.ps1, README.md, comment-based help. | `Get-InforcerSupportedEventType` → sorted list of event type names. `Get-Command -Module InforcerCommunity` lists it. Tab completion on `Get-InforcerAuditEvent -EventType` still works. |
| Invoke-InforcerApiRequest: friendly error messages for HTTP errors | WebException catch block now parses JSON error body and shows API message directly (e.g. "User does not have access to baseline tenant") instead of raw JSON dump. Uses same friendly mapping as success:false handler. | Call any endpoint that returns HTTP 4xx; expect clean single-line error, no raw JSON. |
| Get-InforcerAlignmentDetails: friendly warning for inaccessible baselines | When alignmentDetails endpoint returns forbidden (e.g. shared baseline not yet supported in production), shows `WARNING: No access to alignment details...` instead of raw error. | `Get-InforcerAlignmentDetails -TenantId 14436 -BaselineId <shared-baseline-guid>` → friendly warning. |
| API schema snapshot: UAT environment | `docs/api-schema-snapshot.json` is generated from the UAT environment. The `INFORCER_API_KEY` secret in Powershell-Module-Automation is a UAT key. Dev/UAT URLs and API keys must never be committed to module source code — use GitHub Secrets or workflow env vars only. | Workflow `api-schema-check.yml` uses UAT BaseUrl from secrets/env vars. Snapshot must not contain environment-specific URLs. |
| PS7 HTTP error handling broken in Connect-Inforcer and Invoke-InforcerApiRequest | Both files only caught `[System.Net.WebException]` (PS 5.1). PowerShell 7 throws `HttpResponseException` and puts the response body in `$_.ErrorDetails.Message`. Changed to generic `catch` block that handles both PS5.1 (`WebException`) and PS7 (`ErrorDetails`). Added rate limit detection (HTTP 429, 403 with quota message) → "API rate limit: {message}". | Hit API rate limit → "API rate limit: Out of call volume quota..." instead of raw exception. Connect with bad key → "Connection failed: the API key is invalid". |
| Policy view missing Tags and PolicyData | Format.ps1xml Policy view only showed 6 properties. Added Tags (comma-separated string from EnrichPolicyObject) and PolicyData (compact JSON depth 3). | `Get-InforcerTenantPolicies -TenantId 139` → default output shows Tags and PolicyData. |
| Get-InforcerAlignmentDetails -BaselineId without -TenantId showed duplicate policies | When only -BaselineId was specified, code looped ALL member tenants, causing every baseline policy to appear N times (once per member). Baseline policies are identical regardless of member. Changed to pick first member only. | `Get-InforcerAlignmentDetails -BaselineId "Tier 0"` → 28 rows (not 56). `-TenantId 139 -BaselineId "Tier 0"` → still shows specific tenant comparison. |
| AuditEvent metadata no longer removed from output | Metadata property is kept on the object (not removed). Some event types have event-specific data in metadata (e.g. alertRuleCreate has createAlertRuleConfigCommand). Format.ps1xml controls which properties show by default; metadata is accessible via `$event.metadata` or `Select-Object *`. Flattened fields (ClientIpv4, UserName, UserDisplayName) are still added as top-level properties. | `Get-InforcerAuditEvent` → metadata not shown in default view but accessible via `$event.metadata`. |
| nameLookup key matching too broad | `Add-InforcerPropertyAliases` AuditEvent code matched any key containing "username" or "displayName" in nameLookup, including non-user keys like `alertRuleConfig:displayName:...`. Changed to only match `user:username:*` and `user:displayName:*` patterns using `-like`. | Audit events with alertRule metadata → UserDisplayName is not polluted with alert rule config names. |
| Export-InforcerTenantDocumentation: HTML StringBuilder pattern | For large tenant docs (thousands of policies), `[System.Text.StringBuilder]` with `[void]$sb.AppendLine(...)` is essential. String concatenation (`+=`) in loops causes exponential memory/time degradation. CSS must be embedded inline for self-contained output — no CDN references (IT admin environments often block outbound HTTP). | Generate HTML for a large tenant; verify output is self-contained and renders correctly offline. |
| Export-InforcerTenantDocumentation: `<details>/<summary>` for collapsible sections | HTML5 `<details>/<summary>` elements provide collapsible sections with zero JavaScript. The `name` attribute enables exclusive accordion behavior. Fully supported in all modern browsers. Eliminates need for Bootstrap or custom JS accordion code. | Open exported HTML; verify collapsible sidebar navigation and policy sections work without JavaScript errors. |
| Export-InforcerTenantDocumentation: Graph data resolution requires tenant validation | When resolving group ObjectIDs via Graph, the Graph tenant must match the Inforcer tenant's `msTenantId`. Without this validation, group names could be resolved from the wrong tenant. `Connect-InforcerGraph` handles this by targeting the correct tenant during sign-in. | Use `-FetchGraphData` with a multi-tenant setup; verify group names match the documented tenant, not the admin's home tenant. |
| Export-InforcerTenantDocumentation: Settings Catalog lookup must be O(1) | The IntuneSettingsCatalogViewer `settings.json` is large (~1.4M lines). Loading it with `ConvertFrom-Json -AsHashtable` and re-keying by `id` into a `[hashtable]` gives O(1) lookups per setting. Using `Where-Object` per lookup would be O(n*m) and visibly slow for full tenant exports with thousands of settings. | Export a tenant with many Settings Catalog policies; verify performance is acceptable (seconds, not minutes). |
| Export-InforcerTenantDocumentation: category mapping for non-Intune products | Entra, SharePoint, and M365 Admin Center policies don't have the same product/category structure as Intune. Dedicated mapping tables (Entra 21 settings, SharePoint 11, M365 Admin Center 3) in `Get-InforcerPolicyDisplayInfo` map API internal names to admin portal categories. | Export includes Entra/SharePoint/M365 policies with correct category groupings matching the admin portal. |
| Export-InforcerTenantDocumentation: assignment resolution edge cases | `Resolve-InforcerAssignments` handles multiple `@odata.type` patterns: `#microsoft.graph.allDevicesAssignmentTarget` (All Devices), `#microsoft.graph.allLicensedUsersAssignmentTarget` (All Users), group includes/excludes. Without Graph, shows ObjectIDs; with `-FetchGraphData`, shows display names. Policies with no assignments display "None". | Check exported docs for policies with various assignment types; verify "None" for unassigned policies. |
| Export-InforcerTenantDocumentation: object array rendering | Some policy settings contain arrays of objects (e.g., apps, approvedKeyboards). Previously these rendered as comma-separated empty strings. Fixed to show item count plus individual items with their properties. | Check Settings Catalog policies with array-type settings; verify readable output instead of empty strings. |
| New cmdlet: Get-InforcerUser | Added `Get-InforcerUser` with two ParameterSets (List, ById). List auto-paginates via continuationToken at response root level (not inside .data — required new `-PreserveFullResponse` switch on Invoke-InforcerApiRequest). ById calls `/beta/tenants/{id}/users/{userId}` for full detail. Two PSTypeNames: `InforcerCommunity.UserSummary` (13 aliases) and `InforcerCommunity.User` (~50 aliases, nested objects left as-is). ListControl views for both in Format.ps1xml. | `Get-InforcerUser -TenantId 646` → list of UserSummary objects. `Get-InforcerUser -TenantId 646 -UserId "8e61ce11-..."` → full User detail. `Get-InforcerUser -TenantId 646 -Search "Adele"` → filtered results. |
| Invoke-RestMethod migration (performance) | Replaced `Invoke-WebRequest` + manual `ConvertFrom-Json` with `Invoke-RestMethod` in Invoke-InforcerApiRequest. Eliminates WebResponseObject overhead and redundant JSON parsing. Added response type validation: if `Invoke-RestMethod` returns a string/non-object (e.g. HTML from misconfigured BaseUrl), emits `NonJsonResponse` error instead of crashing on `.PSObject.Properties`. Error handling updated: PS7 uses `$_.ErrorDetails.Message`, PS5.1 fallback reads response stream — generic `catch` block handles both. | All cmdlets noticeably faster. `Connect-Inforcer -ApiKey $key -Region uk; Get-InforcerTenant` → same results, faster. |
| Tenant name resolution in -TenantId | `Resolve-InforcerTenantId` now accepts three formats: numeric ID (direct), GUID (lookup via msTenantId), and tenant name (case-insensitive match on tenantFriendlyName or tenantDnsName). Multiple matches throw with the matching IDs. All cmdlets with -TenantId benefit. `Get-InforcerTenant` passes pre-fetched tenant data via `-TenantData` to avoid duplicate API call. | `Get-InforcerUser -TenantId "Contoso"` → resolves by name. `Get-InforcerTenant -TenantId "Contoso" -Verbose` → single GET /beta/tenants call (not two). |
| PR review: pipeline binding fix | `Get-InforcerUser` TenantId parameter changed from `ValueFromPipeline` + `ValueFromPipelineByPropertyName` to `ValueFromPipelineByPropertyName` only, with `[Alias('ClientTenantId')]`. Prevents whole-object binding when piping (which would call `.ToString()` on the PSObject and fail). Now correctly extracts `ClientTenantId` or `TenantId` property from piped objects. | `Get-InforcerTenant -TenantId 646 \| Get-InforcerUser` → binds ClientTenantId property, not the whole object. |
| PR review: streaming output | List path in Get-InforcerUser now streams items to pipeline immediately instead of buffering all into ArrayList. Only buffers when `-OutputType JsonObject` (needs full array for `ConvertTo-Json`). Reduces memory for large tenants and shows results as pages arrive. | `Get-InforcerUser -TenantId 646` → results appear immediately per page, not after all pages complete. |
| PR review: JsonObject alias duplication | List path was adding PascalCase aliases before the JsonObject branch, causing `ConvertTo-Json` to serialize both camelCase originals and PascalCase aliases (doubled fields). Fixed: JsonObject path buffers raw API items without aliases; PowerShellObject path adds aliases and streams. | `Get-InforcerUser -TenantId 646 -MaxResults 1 -OutputType JsonObject \| ConvertFrom-Json \| Get-Member` → only camelCase properties, no PascalCase duplicates. |
| PR review: Write-Error consistency | Get-InforcerUser error calls updated to include `-ErrorId` and `-Category`: NotConnected/ConnectionError, UserNotFound/ObjectNotFound. Matches the pattern used by all other cmdlets. | `Get-InforcerUser -TenantId 1 -ErrorVariable err -EA SilentlyContinue; $err[0].FullyQualifiedErrorId` → `NotConnected,Get-InforcerUser`. |
| PR review: duplicate API call in Get-InforcerTenant | When -TenantId was a GUID or name, `Resolve-InforcerTenantId` called GET /beta/tenants, then Get-InforcerTenant called it again for output data. Fixed: fetch once, pass to resolver via `-TenantData`, reuse for output. | `Get-InforcerTenant -TenantId "Contoso" -Verbose` → single `Making GET request` line for /beta/tenants. |
| PR review: PreserveFullResponse docs + non-JSON validation | Added `.PARAMETER PreserveFullResponse` help text to Invoke-InforcerApiRequest. Added response type validation: if `Invoke-RestMethod` returns string/non-PSObject (e.g. HTML), emits `NonJsonResponse` error with preview of response content. | Misconfigured BaseUrl returning HTML → clear error message instead of crash on property access. |
| PR review: ById only emits UserNotFound on 404 | `Invoke-InforcerApiRequest` now includes HTTP status code in ErrorId (e.g. `ApiRequestFailed_404`). Get-InforcerUser ById path captures the error and only emits `UserNotFound` when ErrorId confirms 404. For 401/403/500, the API helper's error is the only one shown — no misleading "user not found" on top. | `Get-InforcerUser -TenantId 646 -UserId "bad-guid"` with 404 → "User not found". With 403 → only the API forbidden error, no extra UserNotFound. |
| PR review: Get-InforcerTenant filters before JSON conversion | JsonObject path was converting all tenants to JSON, parsing back, filtering, then re-serializing. Fixed: filter PSObjects first with `Where-Object`, then convert filtered result to JSON once. Avoids unnecessary serialize/deserialize round-trip and potential type coercion. | `Get-InforcerTenant -TenantId 646 -OutputType JsonObject` → correct JSON, single conversion. |
| Pipeline binding applied to all cmdlets | Merge from users branch only applied `ValueFromPipelineByPropertyName` + `[Alias('ClientTenantId')]` to Get-InforcerUser. Post-merge fix applied the same pattern to Get-InforcerTenant, Get-InforcerBaseline, Get-InforcerTenantPolicies, and Get-InforcerAlignmentDetails. Removed `ValueFromPipeline` from Get-InforcerTenant (was binding whole object). | `Get-InforcerTenant \| Get-InforcerTenantPolicies` → binds ClientTenantId by property name. All 5 cmdlets with -TenantId have consistent binding. |
| Compare-InforcerEnvironments: Intune-only scope via category prefix filter | The report must only show Intune-relevant policies. `Get-InforcerTenantPolicies` returns ALL products (Entra ID, SharePoint, Purview, Exchange, etc.) but the comparison report focuses on Intune. **Filter mechanism:** build `$scCategoryPrefixes` from the first segment (before " / ") of all Settings Catalog policy categories. Non-SC policies are only included if their category prefix matches. This dynamically excludes Exchange/* (Defender for Office 365), Entra ID, SharePoint, Purview — without hardcoded product/policy name lists. Products like "Microsoft Defender for Endpoint" contain both Intune SC policies (Windows/*) and Exchange policies (Exchange/*); the category prefix filter correctly includes Windows and excludes Exchange under the same product. **CRITICAL:** Never filter by product name alone — always filter by category prefix. | `Compare-InforcerEnvironments -SourceTenantId A -DestinationTenantId B` → report shows only Intune categories (Windows/*, Android/*, iOS/*, macOS/*). No Exchange, Entra ID, SharePoint, Purview policies. |
| Compare-InforcerEnvironments: tenant name resolution fallback | `tenantFriendlyName` can be null/empty (or literally "Tenant"). Fall back through: tenantFriendlyName → tenantDnsName → numeric ID. Same fallback chain as ConvertTo-InforcerDocModel. Prevents "Tenant Tenant" display in the report header. | Report header shows actual tenant names, not "Tenant" or numeric IDs. |
| Compare-InforcerEnvironments: Settings Catalog deduplication with app context | Multiple `settingDefinitionId`s can resolve to the same friendly name (e.g., "Require that application add-ins are signed by Trusted Publisher" for Word, Excel, PowerPoint). The `$deduplicateNames` scriptblock groups by friendly name and appends the Office application name extracted from the defId (word16v2→Word, excel16v2→Excel, ppt16v2→PowerPoint, etc.). Falls back to a defId segment when no app pattern matches. | Settings like "Allow installation" show as "Allow installation — Edge", "Allow installation — Word", etc. instead of appearing as duplicates. |
| Compare-InforcerEnvironments: non-SC policy comparison shows only differences | When a non-SC policy (compliance, update ring, etc.) is "Conflicting", the collapsible settings detail must only show settings that actually DIFFER between source and destination — not all settings. Showing all settings is noise; the consultant needs to see exactly what changed. | Expand a conflicting compliance policy → only different settings are listed, identical ones are hidden. |
| Compare-InforcerEnvironments: data source is always Get-InforcerTenantPolicies | Both source and destination data always comes from `Get-InforcerTenantPolicies`. No baseline-specific API calls. The cmdlet accepts only `-SourceTenantId` and `-DestinationTenantId` (both mandatory). Cross-account comparison uses `-SourceSession`/`-DestinationSession` from `Connect-Inforcer -PassThru` with session swapping in a `try/finally` block. | `Compare-InforcerEnvironments -SourceTenantId A -DestinationTenantId B` always calls Get-InforcerTenantPolicies for both sides. |
| Compare-InforcerEnvironments: HTML report self-contained with StringBuilder | Same pattern as Export-InforcerTenantDocumentation: `[System.Text.StringBuilder]` assembly, embedded CSS/JS, no CDN references, `prefers-color-scheme` dark/light mode with localStorage toggle, `<details>/<summary>` for collapsible sections, animated alignment score, multi-select filter pills, category dropdown, hide-empty toggle, search across both tabs. | Report opens in any browser offline, toggles dark/light mode, search and filters work on both Comparison and Manual Review tabs. |
| Compare-InforcerEnvironments: alignment score uses one decimal place | `[math]::Round(($counters.Matched / $totalItems) * 100, 1)` — without the decimal, 680/683 rounds to 100% which is misleading. Manual review items are excluded from the score denominator. | 680 of 683 matched → 99.6%, not 100%. |
| Test: Resolve-InforcerTenantId test updated for tenant name support | Old test expected `'not-valid'` to throw as invalid format. After tenant name resolution, `'not-valid'` is now treated as a tenant name lookup. Test renamed to "Throws when tenant name not found" and added `-ErrorAction SilentlyContinue` to suppress the non-terminating `Not connected` error from the API call that interfered with `Should -Throw` in CI. | `Invoke-Pester` → 54/54 pass. The test correctly verifies that unknown tenant names throw `"No tenant found with name..."`. |
| Settings Catalog runtime fetch: replaced bundled settings.json with GitHub Release download | Created `IntuneSettingsCatalogData` repo (github.com/royklo/IntuneSettingsCatalogData) — nightly GitHub Actions workflow fetches from Microsoft Graph and publishes `settings.json` + `categories.json` as GitHub Release assets. New private function `Get-InforcerSettingsCatalogPath` implements 6-tier cache strategy: explicit path → fresh cache (24h TTL) → stale remote check → first-time download → fallback (no catalog) → offline with stale cache. Atomic writes via `.tmp` + `Move-Item`. `Import-InforcerSettingsCatalog` now delegates to this function. Removed bundled `module/data/settings.json` and sibling `IntuneSettingsCatalogViewer` discovery paths from `Export-InforcerTenantDocumentation`. Cache stored at `~/.inforcercommunity/data/`. | `rm -rf ~/.inforcercommunity/data/` then `Import-Module ./module/InforcerCommunity.psd1 -Force; InModuleScope InforcerCommunity { Get-InforcerSettingsCatalogPath -Verbose }` → downloads 62.5 MB settings.json. Run again → "Using fresh cached settings catalog (age: 0h)". `cat ~/.inforcercommunity/data/cache-meta.json` → shows lastChecked, releaseTimestamp, schemaVersion. |

