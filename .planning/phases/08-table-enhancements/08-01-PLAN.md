---
phase: 08-table-enhancements
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - Tests/Renderers.Tests.ps1
autonomous: true
requirements:
  - TBL-01
  - TBL-02
  - TBL-03

must_haves:
  truths:
    - "TBL-01 test verifies HTML output contains .col-resize-handle CSS class and column resize JS function with defaultWidths array"
    - "TBL-02 test verifies rows with settingPath matching a duplicate key render badge-duplicate span with title attribute"
    - "TBL-02 test verifies non-duplicate rows do NOT render badge-duplicate span"
    - "TBL-03 test verifies setting name cell renders strong tag around setting name"
    - "TBL-03 test verifies setting path renders when non-empty even without > separator"
    - "TBL-03 test verifies cell order: strong name then deprecated badge then duplicate badge then setting-path"
    - "All TBL-tagged tests FAIL (RED) before Plan 02 implementation"
  artifacts:
    - path: "Tests/Renderers.Tests.ps1"
      provides: "TBL-01 through TBL-03 Pester test coverage"
      contains: "Describe 'ConvertTo-InforcerComparisonHtml - Table Enhancements'"
  key_links:
    - from: "Tests/Renderers.Tests.ps1"
      to: "module/Private/ConvertTo-InforcerComparisonHtml.ps1"
      via: "ConvertTo-InforcerComparisonHtml function call in BeforeAll"
      pattern: "ConvertTo-InforcerComparisonHtml"
---

<objective>
Write failing Pester tests (RED phase) for all three TBL requirements covering table enhancements in ConvertTo-InforcerComparisonHtml.

Purpose: Establish the test contract before implementation so Plan 02 has clear RED-to-GREEN targets. Tests cover column resize handle rendering and JS (TBL-01), amber duplicate badge with tooltip (TBL-02), and bold name + always-visible path setting name cell layout (TBL-03).

Output: Tests/Renderers.Tests.ps1 with a new Describe block containing tagged It blocks for each TBL requirement. TBL-01, TBL-02, and TBL-03 tests must FAIL (RED state) since the implementation does not exist yet.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-table-enhancements/08-CONTEXT.md
@.planning/phases/08-table-enhancements/08-RESEARCH.md
@.planning/phases/08-table-enhancements/08-UI-SPEC.md

<interfaces>
<!-- Existing test patterns from Renderers.Tests.ps1 -- follow this exact fixture shape -->

ComparisonModel shape for comparison HTML tests (from ENG-03 and Phase 5 test fixtures):

```powershell
# Minimal ComparisonModel with ComparisonRows in Products
$CompModel = @{
    SourceName      = 'Source Tenant'
    DestinationName = 'Dest Tenant'
    Products        = [ordered]@{
        Windows = @{
            Categories = [ordered]@{
                'Settings Catalog' = @{
                    ComparisonRows = [System.Collections.Generic.List[object]]@(
                        @{
                            ItemType     = 'Setting'
                            Name         = 'Some Setting'
                            SettingPath  = 'WiFi > Config'
                            Category     = 'Windows / Settings Catalog'
                            Status       = 'Matched'
                            SourcePolicy = 'Policy A'
                            SourceValue  = 'Value1'
                            DestPolicy   = 'Policy A'
                            DestValue    = 'Value1'
                            IsDeprecated = $false
                        }
                    )
                }
            }
        }
    }
    ManualReview     = [ordered]@{}
    GeneratedAt      = [datetime]::UtcNow
}
```

__DUPLICATE_TABLE__ data contract (from Phase 4):
- ManualReview category key: 'Duplicate Settings (Different Values)'
- Each item has PolicyName, Side, ProfileType, HasDeprecated, and Settings list
- Duplicate settings have Value starting with `__DUPLICATE_TABLE__` prefix
- After stripping prefix, JSON array of @{Policy; Value; Side} objects
- Example: `__DUPLICATE_TABLE__[{"Policy":"Policy A","Value":"true","Side":"Source"},{"Policy":"Policy B","Value":"false","Side":"Destination"}]`

Rendering function call pattern (from existing tests):
```powershell
$html = InModuleScope InforcerCommunity -Parameters @{ Model = $script:CompModel } {
    ConvertTo-InforcerComparisonHtml -ComparisonModel $Model
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write TBL-01 through TBL-03 failing Pester tests</name>
  <files>Tests/Renderers.Tests.ps1</files>
  <read_first>
    - Tests/Renderers.Tests.ps1
    - module/Private/ConvertTo-InforcerComparisonHtml.ps1 (lines 312-331 for table CSS, lines 588-602 for thead structure, lines 680-688 for setting name cell rendering, lines 1185-1205 for JS block end)
    - .planning/phases/08-table-enhancements/08-RESEARCH.md (test map section and Pattern 1/2/3)
    - .planning/phases/08-table-enhancements/08-UI-SPEC.md (component inventory and interaction contract)
  </read_first>
  <behavior>
    TBL-01 (Column resize -- NOT implemented, should FAIL):
    - Test: HTML output contains `.col-resize-handle` in CSS block (style section contains 'col-resize-handle')
    - Test: HTML output contains `th { position: relative` or `th{position:relative` (th rule includes position:relative)
    - Test: JS block contains `defaultWidths` variable for storing initial column widths
    - Test: JS block contains `dblclick` event listener for double-click reset

    TBL-02 (Duplicate badge -- NOT implemented, should FAIL):
    - Test: A ComparisonModel with ManualReview containing 'Duplicate Settings (Different Values)' category where a setting has `__DUPLICATE_TABLE__[{"Policy":"Policy A","Value":"on","Side":"Source"},{"Policy":"Policy B","Value":"off","Side":"Destination"}]` and a ComparisonRow whose SettingPath matches the duplicate setting name (lowercased) renders `badge-duplicate` span in the setting name cell
    - Test: badge-duplicate span has `title` attribute containing "Also configured in:"
    - Test: Non-duplicate rows (SettingPath not in duplicate lookup) do NOT render badge-duplicate span

    TBL-03 (Setting name cell -- NOT implemented, should FAIL):
    - Test: Setting name is wrapped in `<strong>` tag in the td.setting-name cell
    - Test: Setting path renders when non-empty string WITHOUT ` > ` (e.g., just "SimplePathName") -- currently only renders when path contains ` > `
    - Test: Setting path does NOT render when SettingPath is empty string
    - Test: Cell order verification -- strong name appears before badge-deprecated which appears before badge-duplicate which appears before setting-path
  </behavior>
  <action>
    Append a new Describe block to the end of Tests/Renderers.Tests.ps1:

    ```
    Describe 'ConvertTo-InforcerComparisonHtml - Table Enhancements' -Tag 'TBL', 'Phase8' {
    ```

    BeforeAll fixture must include:
    1. A ComparisonModel with TWO ComparisonRows:
       - Row 1: Name='Firewall Mode', SettingPath='Security > Firewall', Status='Conflicting', IsDeprecated=$true, SourcePolicy='Policy A', SourceValue='Block', DestPolicy='Policy A', DestValue='Allow'
       - Row 2: Name='WiFi Standard', SettingPath='SimpleWiFiPath' (no ` > ` -- tests TBL-03 path always rendering), Status='Matched', IsDeprecated=$false, SourcePolicy='Policy B', SourceValue='WPA3', DestPolicy='Policy B', DestValue='WPA3'
       - Row 3: Name='Bluetooth Enabled', SettingPath='' (empty -- tests path omission), Status='Matched', IsDeprecated=$false, SourcePolicy='Policy C', SourceValue='True', DestPolicy='Policy C', DestValue='True'

    2. ManualReview with 'Duplicate Settings (Different Values)' category containing a policy entry whose Settings list includes a setting with:
       - Name = 'security > firewall' (lowercase, matching Row 1's SettingPath.ToLowerInvariant())
       - Value = '__DUPLICATE_TABLE__[{"Policy":"Policy A","Value":"Block","Side":"Source"},{"Policy":"Policy X","Value":"Allow","Side":"Destination"}]'
       This makes Row 1 ('Firewall Mode') appear in the duplicate lookup.

    3. Render HTML once in BeforeAll: `$script:TblHtml = InModuleScope InforcerCommunity -Parameters @{ Model = $script:CompModelTbl } { ConvertTo-InforcerComparisonHtml -ComparisonModel $Model }`

    Test structure (9 It blocks):

    Context 'TBL-01: Column resize' {
        It 'renders col-resize-handle CSS class in style block' -Tag 'TBL-01' {
            $script:TblHtml | Should -Match 'col-resize-handle'
        }
        It 'adds position:relative to th rule' -Tag 'TBL-01' {
            $script:TblHtml | Should -Match 'th\s*\{[^}]*position:\s*relative'
        }
        It 'includes defaultWidths array in JS block' -Tag 'TBL-01' {
            $script:TblHtml | Should -Match 'defaultWidths'
        }
        It 'includes dblclick event for column reset' -Tag 'TBL-01' {
            $script:TblHtml | Should -Match 'dblclick'
        }
    }

    Context 'TBL-02: Duplicate badge' {
        It 'renders badge-duplicate for rows matching duplicate lookup' -Tag 'TBL-02' {
            # Row 1 (Firewall Mode, SettingPath='Security > Firewall') should have badge-duplicate
            $script:TblHtml | Should -Match 'Firewall Mode.*badge-duplicate'
        }
        It 'badge-duplicate has title with Also configured in' -Tag 'TBL-02' {
            $script:TblHtml | Should -Match 'badge-duplicate[^>]*title="Also configured in:'
        }
        It 'does NOT render badge-duplicate for non-duplicate rows' -Tag 'TBL-02' {
            # Row 2 (WiFi Standard) should NOT have badge-duplicate
            # Extract the td containing WiFi Standard and assert no badge-duplicate in it
            $script:TblHtml | Should -Not -Match 'WiFi Standard</strong>\s*<span class="badge-duplicate'
        }
    }

    Context 'TBL-03: Setting name cell layout' {
        It 'wraps setting name in strong tag' -Tag 'TBL-03' {
            $script:TblHtml | Should -Match '<strong>Firewall Mode</strong>'
        }
        It 'renders setting-path for non-empty path without > separator' -Tag 'TBL-03' {
            # WiFi Standard has SettingPath='SimpleWiFiPath' (no > separator)
            $script:TblHtml | Should -Match 'SimpleWiFiPath</span>'
        }
        It 'does not render setting-path span for empty SettingPath' -Tag 'TBL-03' {
            # Bluetooth Enabled has SettingPath=''
            # The td for Bluetooth should not contain setting-path span
            # Use a negative match on the row's content
            $script:TblHtml | Should -Match '<strong>Bluetooth Enabled</strong></td>'
        }
        It 'renders cell elements in order: strong name, deprecated badge, duplicate badge, setting-path' -Tag 'TBL-03' {
            # Row 1 (Firewall Mode) is deprecated AND a duplicate AND has a path
            # Expected order: <strong>Firewall Mode</strong> ... badge-deprecated ... badge-duplicate ... setting-path
            $script:TblHtml | Should -Match '<strong>Firewall Mode</strong>\s*<span class="badge-deprecated.*<span class="badge-duplicate.*<span class="setting-path'
        }
    }

    Commit message: `test(08-01): add failing TBL-01 through TBL-03 Pester tests for table enhancements`
  </action>
  <verify>
    <automated>pwsh -Command "Invoke-Pester ./Tests/Renderers.Tests.ps1 -Tag 'TBL' -PassThru | Select-Object -ExpandProperty FailedCount" -- expect FailedCount > 0 (RED state). Run from repo root.</automated>
  </verify>
  <acceptance_criteria>
    - Tests/Renderers.Tests.ps1 contains `Describe 'ConvertTo-InforcerComparisonHtml - Table Enhancements' -Tag 'TBL', 'Phase8'`
    - Tests/Renderers.Tests.ps1 contains `It 'renders col-resize-handle CSS class in style block' -Tag 'TBL-01'`
    - Tests/Renderers.Tests.ps1 contains `It 'renders badge-duplicate for rows matching duplicate lookup' -Tag 'TBL-02'`
    - Tests/Renderers.Tests.ps1 contains `It 'wraps setting name in strong tag' -Tag 'TBL-03'`
    - Tests/Renderers.Tests.ps1 contains `It 'renders setting-path for non-empty path without > separator' -Tag 'TBL-03'`
    - Tests/Renderers.Tests.ps1 contains `It 'renders cell elements in order: strong name, deprecated badge, duplicate badge, setting-path' -Tag 'TBL-03'`
    - Running `Invoke-Pester ./Tests/Renderers.Tests.ps1 -Tag 'TBL'` shows failures (RED state)
    - At least TBL-01 (all 4 tests), TBL-02 (3 tests), TBL-03 (strong tag + path without >) fail
    - Existing tests (ENG-03, VAL, ASG, MAN) still pass: `Invoke-Pester ./Tests/Renderers.Tests.ps1 -ExcludeTag 'TBL'` exits 0
  </acceptance_criteria>
  <done>New TBL-tagged Describe block with 10 It blocks appended to Renderers.Tests.ps1. All TBL tests fail (RED). All pre-existing tests still pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test fixtures only | Tests use hardcoded fixture data, no untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | N/A | Tests/Renderers.Tests.ps1 | accept | Test-only file -- no trust boundary crossed, no user data processed |
</threat_model>

<verification>
1. `Invoke-Pester ./Tests/Renderers.Tests.ps1 -Tag 'TBL'` runs and shows failures (RED)
2. `Invoke-Pester ./Tests/Renderers.Tests.ps1 -ExcludeTag 'TBL'` runs green (no regression)
3. New Describe block has correct tags: 'TBL', 'Phase8'
</verification>

<success_criteria>
- 10 failing TBL-tagged tests covering TBL-01 (4 tests), TBL-02 (3 tests), TBL-03 (3 tests) plus 1 ordering test
- Zero regressions in existing test suite
- Tests assert exact HTML patterns that Plan 02 must produce
</success_criteria>

<output>
After completion, create `.planning/phases/08-table-enhancements/08-01-SUMMARY.md`
</output>
