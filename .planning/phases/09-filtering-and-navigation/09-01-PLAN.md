---
phase: 09-filtering-and-navigation
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - Tests/Renderers.Tests.ps1
autonomous: true
requirements:
  - FLT-01
  - FLT-02
  - FLT-03
  - FLT-04

must_haves:
  truths:
    - "FLT-01 tests assert data-category attribute uses Product / Category composite format"
    - "FLT-01 tests assert category dropdown option labels use Product / Category composite format"
    - "FLT-02 tests assert filter-summary element has font-size 0.75rem"
    - "FLT-03 tests assert All pill is absent from rendered HTML"
    - "FLT-03 tests assert each status pill has its filter-pill-{status} CSS class"
    - "FLT-03 tests assert CSS block contains filter-pill-matched.active rule"
    - "FLT-04 tests assert search input placeholder text matches D-08"
  artifacts:
    - path: "Tests/Renderers.Tests.ps1"
      provides: "FLT-01 through FLT-04 failing Pester tests"
      contains: "Describe.*Filtering and Navigation.*FLT"
  key_links:
    - from: "Tests/Renderers.Tests.ps1"
      to: "module/Private/ConvertTo-InforcerComparisonHtml.ps1"
      via: "ConvertTo-InforcerComparisonHtml -ComparisonModel"
      pattern: "ConvertTo-InforcerComparisonHtml"
---

<objective>
Write failing Pester tests (RED phase) for FLT-01 through FLT-04 filtering and navigation requirements.

Purpose: Establish the test contract before implementation. Tests define the exact HTML output expectations for category dropdown format, filter summary styling, status pill classes, and search placeholder.
Output: A new `Describe` block in Tests/Renderers.Tests.ps1 with tagged tests that all FAIL against the current codebase.
</objective>

<execution_context>
@/Users/roy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/roy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-filtering-and-navigation/09-CONTEXT.md
@.planning/phases/09-filtering-and-navigation/09-RESEARCH.md
@.planning/phases/09-filtering-and-navigation/09-UI-SPEC.md

<interfaces>
<!-- Existing test pattern from Phase 8 TBL tests (Renderers.Tests.ps1 lines 935-1054) -->
<!-- Executor must follow the same fixture + InModuleScope + tag pattern -->

From Tests/Renderers.Tests.ps1 (Phase 8 pattern):
```powershell
Describe 'ConvertTo-InforcerComparisonHtml - Table Enhancements' -Tag 'TBL', 'Phase8' {
    BeforeAll {
        $script:CompModelTbl = @{
            SourceName      = 'Source Tenant'
            DestinationName = 'Dest Tenant'
            Products        = [ordered]@{
                Windows = @{
                    Categories = [ordered]@{
                        'Settings Catalog' = @{
                            ComparisonRows = [System.Collections.Generic.List[object]]@(...)
                        }
                    }
                }
            }
            ManualReview = [ordered]@{}
            GeneratedAt  = [datetime]::UtcNow
        }
        $script:TblHtml = InModuleScope InforcerCommunity -Parameters @{ Model = $script:CompModelTbl } {
            ConvertTo-InforcerComparisonHtml -ComparisonModel $Model
        }
    }
    Context 'TBL-01: Column resize' {
        It 'renders col-resize-handle CSS class in style block' -Tag 'TBL-01' { ... }
    }
}
```

From module/Private/ConvertTo-InforcerComparisonHtml.ps1 (current state):
- Line 526: `<input ... placeholder="Search policies, settings, values...">`
- Line 538: Category stripping: `if ($cat -match '^[^/]+\s*/\s*(.+)$') { $cat = $Matches[1] }`
- Line 547: `<button class="filter-pill active" onclick="filterByStatus(this,''All'')">All</button>`
- Line 548-551: Status pills without per-status CSS classes
- Line 561: `font-size:0.9rem` on filter-summary div
- Line 693-695: Row rendering strips product prefix from category
- Line 704: `data-category="$encCategory"` uses stripped category
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing FLT-01 through FLT-04 Pester tests</name>
  <files>Tests/Renderers.Tests.ps1</files>
  <read_first>
    - Tests/Renderers.Tests.ps1 (current test file — append after the TBL Describe block)
    - module/Private/ConvertTo-InforcerComparisonHtml.ps1 (lines 524-561 for current filter bar HTML, lines 232-238 for current CSS, lines 691-704 for row rendering)
    - .planning/phases/09-filtering-and-navigation/09-CONTEXT.md (D-01 through D-08 locked decisions)
    - .planning/phases/09-filtering-and-navigation/09-UI-SPEC.md (exact CSS values and copy)
  </read_first>
  <behavior>
    FLT-01 (Category dropdown — D-01, D-02):
    - Test 1: data-category attribute on rendered rows contains "Intune / Settings Catalog" (the Product / stripped-category composite), NOT just "Settings Catalog"
    - Test 2: Category dropdown option value contains "Intune / Settings Catalog" composite format
    - Test 3: A second product "Entra" produces data-category="Entra / Conditional Access" (multi-product coverage)

    FLT-02 (Filter summary — D-03):
    - Test 4: filter-summary div has font-size:0.75rem (not 0.9rem per UI-SPEC correction)

    FLT-03 (Status pills — D-04, D-05, D-06):
    - Test 5: HTML does NOT contain `onclick="filterByStatus(this,''All'')"` (All pill removed per D-06)
    - Test 6: HTML contains `class="filter-pill filter-pill-matched"` on the Matched pill
    - Test 7: HTML contains `class="filter-pill filter-pill-conflicting"` on the Conflicting pill
    - Test 8: HTML contains `class="filter-pill filter-pill-source-only"` on the Source Only pill
    - Test 9: HTML contains `class="filter-pill filter-pill-dest-only"` on the Dest Only pill
    - Test 10: CSS block contains `.filter-pill-matched.active` rule with `var(--success)`
    - Test 11: CSS block contains `.filter-pill-conflicting.active` rule with `var(--danger)`
    - Test 12: CSS block contains `.hidden` rule with `display: none !important`

    FLT-04 (Search — D-07, D-08):
    - Test 13: Search input placeholder text is "Search setting name, path, values, policies, category..."
  </behavior>
  <action>
    Append a new Describe block to Tests/Renderers.Tests.ps1 after the existing TBL Describe block (after line ~1090):

    ```powershell
    Describe 'ConvertTo-InforcerComparisonHtml - Filtering and Navigation' -Tag 'FLT', 'Phase9' {
    ```

    **Fixture design:** Create a ComparisonModel with TWO products to verify multi-product category composites:
    - Product "Intune" with category "Settings Catalog" containing one ComparisonRow where `$row.Category = 'Intune / Windows / Settings Catalog'` (the raw category from the engine includes the product prefix — confirmed by RESEARCH A1). The composite should produce `data-category="Intune / Windows / Settings Catalog"`.
    - Product "Entra" with category "Conditional Access" containing one ComparisonRow where `$row.Category = 'Entra / Conditional Access / Policies'`. The composite should produce `data-category="Entra / Conditional Access / Policies"`.

    **Important note on category format:** The raw `$row.Category` value from the engine already includes the full product-prefixed string (e.g., "Intune / Windows / Settings Catalog"). Currently the code STRIPS the product prefix, producing just "Windows / Settings Catalog". After Phase 9, it should KEEP the full value as-is in `data-category`. The tests should assert the FULL value is present.

    Actually, re-reading the research more carefully: D-01 says to build "Product / Category" composites using `$productName` from the outer loop. The current `$row.Category` may contain "Intune / Windows / Settings Catalog" — the stripping removes "Intune / " to get "Windows / Settings Catalog". D-01/D-02 want the dropdown to show "Product / Category" = the full value. So the simplest approach per D-01 is: if `$row.Category` already has the full product prefix, just STOP stripping it and use it directly. The test fixture should set `$row.Category` to values like `'Intune / Windows / Settings Catalog'` and assert that `data-category` contains that exact full value.

    Use the standard InModuleScope pattern:
    ```powershell
    $script:FltHtml = InModuleScope InforcerCommunity -Parameters @{ Model = $script:CompModelFlt } {
        ConvertTo-InforcerComparisonHtml -ComparisonModel $Model
    }
    ```

    Organize tests into Context blocks per requirement:
    - `Context 'FLT-01: Category dropdown'` with -Tag 'FLT-01'
    - `Context 'FLT-02: Filter summary'` with -Tag 'FLT-02'
    - `Context 'FLT-03: Status pills'` with -Tag 'FLT-03'
    - `Context 'FLT-04: Search'` with -Tag 'FLT-04'

    All tests MUST fail against the current codebase (RED phase). Verify by running them.
  </action>
  <verify>
    <automated>cd /Users/roy/github/royklo/InforcerCommunity && pwsh -Command "Invoke-Pester ./Tests/Renderers.Tests.ps1 -Tag 'FLT' -PassThru | ForEach-Object { Write-Host \"Passed: $($_.PassedCount) Failed: $($_.FailedCount)\"; if ($_.FailedCount -eq 0) { throw 'RED phase violation: all tests passed but should fail' } }"</automated>
  </verify>
  <done>
    - Tests/Renderers.Tests.ps1 contains a new Describe block tagged 'FLT', 'Phase9'
    - At least 10 tests exist covering FLT-01 through FLT-04
    - ALL tests FAIL when run against the current codebase (RED phase confirmed)
    - No existing tests are broken (TBL, MAN, VAL tests still pass)
  </done>
  <acceptance_criteria>
    - Tests/Renderers.Tests.ps1 contains `Describe 'ConvertTo-InforcerComparisonHtml - Filtering and Navigation' -Tag 'FLT', 'Phase9'`
    - Tests/Renderers.Tests.ps1 contains `Context 'FLT-01: Category dropdown'`
    - Tests/Renderers.Tests.ps1 contains `Context 'FLT-02: Filter summary'`
    - Tests/Renderers.Tests.ps1 contains `Context 'FLT-03: Status pills'`
    - Tests/Renderers.Tests.ps1 contains `Context 'FLT-04: Search'`
    - Tests/Renderers.Tests.ps1 contains `data-category.*Intune / Windows / Settings Catalog` assertion
    - Tests/Renderers.Tests.ps1 contains `filter-pill-matched` assertion
    - Tests/Renderers.Tests.ps1 contains `font-size.*0\.75rem` or `font-size:0\.75rem` assertion
    - Tests/Renderers.Tests.ps1 contains `Search setting name, path, values, policies, category` assertion
    - `Invoke-Pester ./Tests/Renderers.Tests.ps1 -Tag 'FLT'` reports FailedCount > 0 (RED)
    - `Invoke-Pester ./Tests/Renderers.Tests.ps1 -Tag 'TBL'` reports FailedCount = 0 (existing tests unbroken)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API data -> HTML output | Category names and policy names from Inforcer API rendered into HTML attributes and element text |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | S (Spoofing) | data-category attribute | accept | Static local report file, no network access. Category values sourced from authenticated API. |
| T-09-02 | T (Tampering) | Injected script via category name | mitigate | All category values pass through `[System.Net.WebUtility]::HtmlEncode()` before insertion into HTML attributes and option values. Existing pattern verified at lines 556, 695. Tests in this plan only read output HTML, no new attack surface. |
</threat_model>

<verification>
1. `Invoke-Pester ./Tests/Renderers.Tests.ps1 -Tag 'FLT'` — all FLT tests FAIL (RED phase)
2. `Invoke-Pester ./Tests/Renderers.Tests.ps1 -Tag 'TBL','MAN','VAL'` — all prior tests PASS (no regression)
</verification>

<success_criteria>
- Failing test suite establishes the contract for Plan 02 implementation
- Every FLT requirement has at least 2 test assertions
- Test fixture uses multi-product ComparisonModel for realistic coverage
</success_criteria>

<output>
After completion, create `.planning/phases/09-filtering-and-navigation/09-01-SUMMARY.md`
</output>
