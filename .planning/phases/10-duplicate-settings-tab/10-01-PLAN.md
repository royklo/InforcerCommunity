---
phase: 10-duplicate-settings-tab
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - Tests/Renderers.Tests.ps1
autonomous: true
requirements:
  - DUP-01
  - DUP-02
  - DUP-03
  - DUP-04
must_haves:
  truths:
    - "Pester tests for DUP-01 through DUP-04 exist and FAIL against the current codebase (TDD RED)"
    - "Test fixture contains a ComparisonModel with 'Duplicate Settings (Different Values)' ManualReview entries using __DUPLICATE_TABLE__ JSON"
    - "A second fixture with no duplicates verifies the tab is hidden"
  artifacts:
    - path: "Tests/Renderers.Tests.ps1"
      provides: "Failing tests for Duplicates tab rendering"
      contains: "Describe 'ConvertTo-InforcerComparisonHtml - Duplicates Tab'"
  key_links:
    - from: "Tests/Renderers.Tests.ps1"
      to: "module/Private/ConvertTo-InforcerComparisonHtml.ps1"
      via: "InModuleScope ConvertTo-InforcerComparisonHtml -ComparisonModel"
      pattern: "ConvertTo-InforcerComparisonHtml"
---

<objective>
Write failing Pester 5.x tests (TDD RED) for the Duplicates tab in ConvertTo-InforcerComparisonHtml.ps1, covering all four requirements (DUP-01 through DUP-04).

Purpose: Establish the test contract before implementation. Every test must fail against the current codebase because the Duplicates tab does not exist yet.
Output: New Describe block in Tests/Renderers.Tests.ps1 with 15+ failing tests tagged DUP-01 through DUP-04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-duplicate-settings-tab/10-CONTEXT.md
@.planning/phases/10-duplicate-settings-tab/10-RESEARCH.md
@.planning/phases/10-duplicate-settings-tab/10-UI-SPEC.md

<interfaces>
<!-- From Tests/Renderers.Tests.ps1 — existing patterns the new tests must follow -->

BeforeAll block (lines 7-12):
```powershell
Remove-Module -Name 'InforcerCommunity' -ErrorAction SilentlyContinue
$here = $PSScriptRoot
$manifestPath = [System.IO.Path]::GetFullPath((Join-Path $here '..' 'module' 'InforcerCommunity.psd1'))
Import-Module $manifestPath -Force -ErrorAction Stop
```

Existing ComparisonModel fixture pattern (from MAN tests, lines 746-833):
```powershell
$script:CompModelMan = @{
    SourceName      = 'Tenant A'
    DestinationName = 'Tenant B'
    GeneratedAt     = '2026-01-01'
    AlignmentScore  = 85
    TotalItems      = 10
    Counters        = @{ Matched = 8; Conflicting = 1; SourceOnly = 1; DestOnly = 0 }
    StatusCounts    = @{ Matched = 8; Conflicting = 1; SourceOnly = 1; DestOnly = 0 }
    Products        = [ordered]@{}
    ManualReview    = [ordered]@{ ... }
}
$script:ManHtml = InModuleScope InforcerCommunity -Parameters @{ Model = $script:CompModelMan } {
    ConvertTo-InforcerComparisonHtml -ComparisonModel $Model
}
```

Duplicate data format (Phase 4 contract):
- ManualReview key: 'Duplicate Settings (Different Values)'
- Each item: @{ PolicyName; Side; ProfileType; Settings = @(@{ Name; Value = '__DUPLICATE_TABLE__[JSON]'; IsDeprecated }) }
- JSON payload: [{"Policy":"...","Value":"...","Side":"Source|Destination"}]
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing Pester tests for DUP-01 through DUP-04</name>
  <files>Tests/Renderers.Tests.ps1</files>
  <read_first>
    - Tests/Renderers.Tests.ps1 (full file — understand fixture patterns, existing Describe blocks, tag conventions)
    - module/Private/ConvertTo-InforcerComparisonHtml.ps1 lines 565-580 (tab nav rendering pattern)
    - module/Private/ConvertTo-InforcerComparisonHtml.ps1 lines 660-680 (duplicate lookup parse)
    - module/Private/ConvertTo-InforcerComparisonHtml.ps1 lines 1178-1184 (switchTab JS function)
    - .planning/phases/10-duplicate-settings-tab/10-CONTEXT.md (locked decisions D-01 through D-12)
    - .planning/phases/10-duplicate-settings-tab/10-UI-SPEC.md (CSS class names, copywriting, interaction specs)
  </read_first>
  <action>
Append a new Describe block at the end of Tests/Renderers.Tests.ps1 (before the final closing if any, after the last existing Describe block which ends at approximately line 1233).

**Describe block name:** `'ConvertTo-InforcerComparisonHtml - Duplicates Tab'` with `-Tag 'DuplicatesTab'`

**BeforeAll — Two fixtures:**

1. `$script:DupModel` — ComparisonModel WITH duplicates:
   - SourceName = 'Tenant A', DestinationName = 'Tenant B'
   - GeneratedAt = '2026-01-01', AlignmentScore = 75, TotalItems = 5
   - Counters/StatusCounts = @{ Matched = 3; Conflicting = 1; SourceOnly = 1; DestOnly = 0 }
   - Products = [ordered]@{} (empty — we only need ManualReview for these tests)
   - ManualReview with 'Duplicate Settings (Different Values)' containing 3 duplicate items:
     - Item 1 (same-tenant scenario): Setting name `'deviceLock_maxMinutesOfInactivity'`, two policies same side:
       `'__DUPLICATE_TABLE__[{"Policy":"Baseline Security","Value":"5","Side":"Source"},{"Policy":"Strict Policy","Value":"15","Side":"Source"}]'`
     - Item 2 (cross-tenant scenario): Setting name `'firewall_enableFirewall'`, policies both sides:
       `'__DUPLICATE_TABLE__[{"Policy":"Corp Firewall","Value":"true","Side":"Source"},{"Policy":"Corp Firewall","Value":"true","Side":"Destination"},{"Policy":"Legacy FW","Value":"false","Side":"Source"}]'`
     - Item 3 (all-unique scenario): Setting name `'bitlocker_encryptionMethod'`, three policies all different:
       `'__DUPLICATE_TABLE__[{"Policy":"Policy A","Value":"xtsAes128","Side":"Source"},{"Policy":"Policy B","Value":"xtsAes256","Side":"Destination"},{"Policy":"Policy C","Value":"aesCbc128","Side":"Source"}]'`
   - Each ManualReview item wrapped in the standard structure: @{ PolicyName = '...'; Side = 'Source'; ProfileType = 'Settings Catalog'; Settings = @(@{ Name = '...'; Value = '__DUPLICATE_TABLE__...'; IsDeprecated = $false }); HasDeprecated = $false }

   Generate HTML: `$script:DupHtml = InModuleScope InforcerCommunity -Parameters @{ Model = $script:DupModel } { ConvertTo-InforcerComparisonHtml -ComparisonModel $Model }`

2. `$script:NoDupModel` — ComparisonModel WITHOUT duplicates:
   - Same structure but ManualReview = [ordered]@{} (empty)
   - Generate HTML: `$script:NoDupHtml = InModuleScope InforcerCommunity -Parameters @{ Model = $script:NoDupModel } { ConvertTo-InforcerComparisonHtml -ComparisonModel $Model }`

**Context blocks and It tests (all expected to FAIL in RED phase):**

Context 'DUP-01: Tab button and content' {
  1. `It 'renders Duplicates tab button when duplicates exist' -Tag 'DUP-01'` — assert `$DupHtml` matches `switchTab\('duplicates'\)` (per D-01)
  2. `It 'Duplicates tab button shows count badge with 3' -Tag 'DUP-01'` — assert `$DupHtml` matches `Duplicates.*<span.*status-badge.*>3</span>` (3 unique settings per D-01)
  3. `It 'does NOT render Duplicates tab button when no duplicates' -Tag 'DUP-01'` — assert `$NoDupHtml` does NOT match `switchTab\('duplicates'\)` (per D-02)
  4. `It 'renders tab-duplicates content div when duplicates exist' -Tag 'DUP-01'` — assert `$DupHtml` matches `id="tab-duplicates"`
  5. `It 'does NOT render tab-duplicates div when no duplicates' -Tag 'DUP-01'` — assert `$NoDupHtml` does NOT match `tab-duplicates`
  6. `It 'renders amber info banner with warning text' -Tag 'DUP-01'` — assert `$DupHtml` matches `dup-info-banner` and `Duplicate Settings Detected` (per D-03, UI-SPEC copywriting)
}

Context 'DUP-02: Three-column table with policy entries' {
  7. `It 'renders dup-tab-table with three column headers' -Tag 'DUP-02'` — assert `$DupHtml` matches `dup-tab-table` and contains `>Setting<` and `>Policies &amp; Values<` and `>Analysis<` (per D-04)
  8. `It 'renders setting name deviceLock_maxMinutesOfInactivity in table row' -Tag 'DUP-02'` — assert `$DupHtml` matches `deviceLock_maxMinutesOfInactivity`
  9. `It 'renders Source side badge for source policy' -Tag 'DUP-02'` — assert `$DupHtml` matches `side-badge side-source.*Source` within the duplicates tab context (per D-05, D-06)
  10. `It 'renders Destination side badge for destination policy' -Tag 'DUP-02'` — assert `$DupHtml` matches `side-badge side-dest.*Destination` (per D-05, D-06)
  11. `It 'renders policy values in dup-policy-value class' -Tag 'DUP-02'` — assert `$DupHtml` matches `dup-policy-value`
}

Context 'DUP-03: Analysis messaging via analyzeDuplicate JS' {
  12. `It 'HTML contains analyzeDuplicate JavaScript function' -Tag 'DUP-03'` — assert `$DupHtml` matches `function analyzeDuplicate\(` (per D-07)
  13. `It 'table rows contain data-policies-json attribute with valid JSON' -Tag 'DUP-03'` — assert `$DupHtml` matches `data-policies-json="` (per D-09)
  14. `It 'analysis cells have dup-analysis-text class' -Tag 'DUP-03'` — assert `$DupHtml` matches `dup-analysis-text` (per D-08)
}

Context 'DUP-04: Duplicate tab search' {
  15. `It 'renders search input with correct placeholder' -Tag 'DUP-04'` — assert `$DupHtml` matches `placeholder="Search settings or policies\.\.\."` (per D-10)
  16. `It 'HTML contains dupTabSearch JavaScript function' -Tag 'DUP-04'` — assert `$DupHtml` matches `function dupTabSearch\(` (per D-11)
  17. `It 'renders summary line with dup-summary id' -Tag 'DUP-04'` — assert `$DupHtml` matches `id="dup-summary"` (per D-12)
  18. `It 'table rows contain data-setting and data-policies attributes for JS filtering' -Tag 'DUP-04'` — assert `$DupHtml` matches `data-setting="` and `data-policies="` (per D-11)
}

Context 'DUP-01: CSS classes' {
  19. `It 'CSS block contains dup-tab-table class' -Tag 'DUP-01'` — assert `$DupHtml` matches `\.dup-tab-table\s*\{` or `.dup-tab-table {`
  20. `It 'CSS block contains dup-info-banner class' -Tag 'DUP-01'` — assert `$DupHtml` matches `\.dup-info-banner\s*\{`
  21. `It 'CSS block contains dup-policy-value class with monospace font' -Tag 'DUP-02'` — assert `$DupHtml` matches `\.dup-policy-value\s*\{[^}]*monospace`
}

All tests use `$script:DupHtml | Should -Match '...'` or `Should -Not -Match` patterns.
  </action>
  <verify>
    <automated>cd /Users/roy/github/royklo/InforcerCommunity && pwsh -Command "Invoke-Pester ./Tests/Renderers.Tests.ps1 -Tag 'DuplicatesTab' -PassThru | Select-Object -Property TotalCount, PassedCount, FailedCount"</automated>
  </verify>
  <acceptance_criteria>
    - Tests/Renderers.Tests.ps1 contains `Describe 'ConvertTo-InforcerComparisonHtml - Duplicates Tab'`
    - Tests/Renderers.Tests.ps1 contains `-Tag 'DuplicatesTab'`
    - Tests/Renderers.Tests.ps1 contains `-Tag 'DUP-01'` in at least 5 It blocks
    - Tests/Renderers.Tests.ps1 contains `-Tag 'DUP-02'` in at least 4 It blocks
    - Tests/Renderers.Tests.ps1 contains `-Tag 'DUP-03'` in at least 3 It blocks
    - Tests/Renderers.Tests.ps1 contains `-Tag 'DUP-04'` in at least 4 It blocks
    - Tests/Renderers.Tests.ps1 contains `__DUPLICATE_TABLE__` in the fixture
    - Tests/Renderers.Tests.ps1 contains `switchTab.*duplicates` in a Should -Match assertion
    - Tests/Renderers.Tests.ps1 contains `analyzeDuplicate` in a Should -Match assertion
    - Tests/Renderers.Tests.ps1 contains `dupTabSearch` in a Should -Match assertion
    - Tests/Renderers.Tests.ps1 contains `dup-info-banner` in a Should -Match assertion
    - Tests/Renderers.Tests.ps1 contains `data-policies-json` in a Should -Match assertion
    - Pester run with -Tag 'DuplicatesTab' shows FailedCount >= 15 (all tests FAIL — RED)
    - Pester run with -Tag 'DuplicatesTab' shows PassedCount of 2 (only the Should -Not -Match tests for NoDupHtml pass because the tab truly doesn't exist yet)
  </acceptance_criteria>
  <done>All DUP-01 through DUP-04 tests exist and fail against the current codebase (TDD RED). The two "should NOT match" tests for NoDupHtml pass trivially.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none | This plan only modifies test files — no production code or HTML output changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | Test fixture data attributes | accept | Test fixtures use hardcoded safe strings — no user input flows through tests |
</threat_model>

<verification>
Run `Invoke-Pester ./Tests/Renderers.Tests.ps1 -Tag 'DuplicatesTab'` — all DUP-tagged tests must exist and the majority must FAIL (RED phase). Only the 2 "should NOT match on NoDupHtml" tests should pass.
</verification>

<success_criteria>
- 21 test cases tagged DUP-01 through DUP-04 exist in Renderers.Tests.ps1
- `Invoke-Pester -Tag 'DuplicatesTab'` runs without parse errors
- FailedCount >= 15 (TDD RED confirmed)
- PassedCount = 2 (NoDupHtml negative assertions pass trivially)
</success_criteria>

<output>
After completion, create `.planning/phases/10-duplicate-settings-tab/10-01-SUMMARY.md`
</output>
